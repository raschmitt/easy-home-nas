# Matches Nextcloud's own failed-login log lines (nextcloud.log, JSON lines
# format enabled by default since NC 20+). Nextcloud already throttles brute
# force internally; this jail adds an IP-level ban on top of that.
[Definition]
failregex = ^.*"Login failed: '.*' \(Remote IP: '<HOST>'\)".*$
ignoreregex =
